# 天问-AGI 演示脚本

> 用于演示智能体核心能力的可执行脚本

## 演示 1：代码生成

```
用户: 写一个JavaScript快速排序算法

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
认知引擎:
- 意图识别: Execute.Develop.Code (置信度 0.95)
- 实体提取: 语言=JavaScript, 算法=快速排序
- 上下文: 无相关上下文
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
规划引擎:
- 任务类型: 代码生成
- 分解步骤: 
  1. 定义函数签名
  2. 实现分区逻辑
  3. 递归调用
- 依赖分析: 无依赖
- 预计时长: 5分钟
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
执行引擎:
- 调用技能: DSA.md
- 工具: 代码生成
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

输出:

```javascript
/**
 * 快速排序算法
 * 时间复杂度: O(n log n) 平均, O(n²) 最坏
 * 空间复杂度: O(log n)
 */
function quickSort(arr) {
  // 基线条件
  if (arr.length <= 1) return arr;

  // 选择基准元素
  const pivot = arr[Math.floor(arr.length / 2)];
  const left = [];
  const middle = [];
  const right = [];

  // 分区操作
  for (const item of arr) {
    if (item < pivot) {
      left.push(item);
    } else if (item === pivot) {
      middle.push(item);
    } else {
      right.push(item);
    }
  }

  // 递归组合
  return [...quickSort(left), ...middle, ...quickSort(right)];
}

// 测试
const arr = [64, 34, 25, 12, 22, 11, 90];
console.log(quickSort(arr)); // [11, 12, 22, 25, 34, 64, 90]
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 代码生成完成
评分: 意图识别✓ 任务分解✓ 输出质量✓
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 演示 2：复杂任务规划

```
用户: 创建一个完整的在线商城的用户模块

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
认知引擎:
- 意图识别: Execute.Develop.Fullstack.Module (置信度 0.92)
- 实体提取: 模块=用户模块, 范围=完整
- 复杂度评估: 高
- 所需技能: [Product, Database, API-Design, Backend, Frontend, Testing]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
规划引擎:
- 任务分解:

┌─────────────────────────────────────────────┐
│ 阶段1: 需求分析                              │
│ - 技能: Product                            │
│ - 输出: PRD文档                            │
│ - 时长: 2小时                              │
└─────────────────────────────────────────────┘
                    ↓ (依赖)
┌─────────────────────────────────────────────┐
│ 阶段2: 技术设计                            │
│ - 数据库设计 (Database)                    │
│ - API设计 (API-Design)                     │
│ - 时长: 4小时                              │
│ - 两者可并行                               │
└─────────────────────────────────────────────┘
                    ↓ (依赖)
┌─────────────────────────────────────────────┐
│ 阶段3: 开发实现                            │
│ - 后端开发 (Backend)                       │
│ - 前端开发 (Frontend)                      │
│ - 时长: 16小时 (可并行)                     │
└─────────────────────────────────────────────┘
                    ↓ (依赖)
┌─────────────────────────────────────────────┐
│ 阶段4: 测试验收                            │
│ - 技能: Testing                            │
│ - 时长: 4小时                              │
└─────────────────────────────────────────────┘

- 总工期: 约 26 小时 (3-4天)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
执行引擎:
- 调度: Product-Manager
- 技能链: Product → Database + API-Design → Backend + Frontend → Testing
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

阶段1详细计划:

## 用户模块 PRD

### 功能需求
1. 用户注册/登录
2. 个人信息管理
3. 收货地址管理
4. 会员等级

### 数据模型
```
User {
  id: UUID
  username: String
  email: String
  phone: String
  level: Enum
  createdAt: DateTime
}

Address {
  id: UUID
  userId: FK
  name: String
  phone: String
  province: String
  city: String
  district: String
  detail: String
  isDefault: Boolean
}
```

### API设计
```
POST   /api/users/register    # 注册
POST   /api/users/login     # 登录
GET    /api/users/profile    # 获取个人信息
PATCH  /api/users/profile    # 更新个人信息
POST   /api/users/address   # 添加地址
GET    /api/users/address   # 获取地址列表
DELETE /api/users/address/:id # 删除地址
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 任务规划完成
评分: 意图识别✓ 任务分解✓ 规划合理性✓
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 演示 3：情感交互

```
用户: 这个bug调了一天了，还是找不到问题在哪，好烦躁啊

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
情感分析:
- 主要情感: 沮丧 (强度 0.85)
- 次要情感: 焦虑 (强度 0.60)
- 情感倾向: 负面
- 触发词: "烦躁"、"找不到"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
情感响应策略:
- 策略: 同理 + 实际帮助
- 语气: 温暖、专业
- 先安抚情绪，再解决问题
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
执行引擎:
- 调用技能: Emotional-Understanding.md, Debugging.md
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

输出:

我理解你的感受！花了一整天调试一个问题还没解决，真的很让人沮丧。

别着急，我们一起来梳理一下可能的原因：

**🔍 系统性排查步骤：**

1. **确认问题边界**
   - 这个bug是必现还是偶现？
   - 是最近代码改动引入的还是之前就有？

2. **缩小问题范围**
   - 在哪个环节出错？
   - 能否通过打印日志找到位置？

3. **常见原因检查**
   - [ ] 变量未定义或作用域问题
   - [ ] 异步操作未正确处理 (await/then)
   - [ ] 数据类型转换问题
   - [ ] 空指针/空数组访问

**💡 建议尝试：**

```
// 添加日志定位问题
console.log("步骤1", data);
const result = process(data);
console.log("步骤2", result);
console.log("步骤3", result.value);
```

**能告诉我具体是什么错误信息吗？** 我可以帮你更精准地定位问题。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 情感交互完成
评分: 情感识别✓ 响应策略✓ 同理表达✓
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 演示 4：安全审查

```
用户: 检查这段登录代码的安全性

```javascript
const login = (email, password) => {
  const query = `SELECT * FROM users WHERE email = '${email}'`;
  db.query(query);
  return user.password === password;
};
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
认知引擎:
- 意图识别: Execute.Analyze.Security (置信度 0.94)
- 实体提取: 代码=登录逻辑, 语言=JavaScript
- 复杂度: 中
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
安全分析 (调用 Security.md):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 🔴 安全漏洞报告

### 漏洞 1: SQL注入 (严重)
**位置**: 第3行
**问题**: 直接拼接用户输入到SQL查询

```javascript
// 危险代码
const query = `SELECT * FROM users WHERE email = '${email}'`;
```

**攻击示例**:
```
email: "admin' OR '1'='1"
执行: SELECT * FROM users WHERE email = 'admin' OR '1'='1'
结果: 返回所有用户!
```

**修复方案**:
```javascript
// 使用参数化查询
const query = 'SELECT * FROM users WHERE email = $1';
db.query(query, [email]);
```

---

### 漏洞 2: 密码明文比较 (严重)
**位置**: 第4行
**问题**: 直接比较密码，存在时序攻击风险

**修复方案**:
```javascript
const bcrypt = require('bcrypt');
const isMatch = await bcrypt.compare(password, user.password);
```

---

### 漏洞 3: 密码存储 (高)
**问题**: 未检查密码是否使用强哈希存储

**修复方案**:
```javascript
// 注册时哈希密码
const hash = await bcrypt.hash(password, 12);
```

---

## 安全建议

1. ✅ 使用参数化查询防注入
2. ✅ 使用 bcrypt 比较密码
3. ✅ 添加登录失败次数限制
4. ✅ 使用 HTTPS 加密传输
5. ✅ 添加日志审计

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 安全审查完成
发现漏洞: 3个 (1严重, 1严重, 1高)
修复方案: 全部提供
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 演示 5：架构设计

```
用户: 设计一个高并发的秒杀系统的架构

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
认知引擎:
- 意图识别: Execute.Design.Architecture (置信度 0.93)
- 实体提取: 系统=秒杀系统, 特性=高并发
- 复杂度: 极高
- 关键挑战: 流量大、库存扣减一致性
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
架构设计 (调用 Architecture.md):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 秒杀系统架构设计

### 系统架构图

```
                    ┌─────────────┐
                    │   CDN       │
                    └──────┬──────┘
                           │
                    ┌──────┴──────┐
                    │   LVS       │
                    │  (负载均衡) │
                    └──────┬──────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   ┌────┴────┐       ┌────┴────┐       ┌────┴────┐
   │  Nginx  │       │  Nginx  │       │  Nginx  │
   │  集群    │       │  集群    │       │  集群    │
   └────┬────┘       └────┬────┘       └────┬────┘
        │                  │                  │
        └──────────────────┼──────────────────┘
                             │
                    ┌────────┴────────┐
                    │   API Gateway   │
                    │  (限流/鉴权)    │
                    └────────┬────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
   ┌────┴────┐          ┌────┴────┐          ┌────┴────┐
   │  秒杀   │          │  秒杀   │          │  秒杀   │
   │ 服务1   │          │ 服务2   │          │ 服务3   │
   └────┬────┘          └────┬────┘          └────┬────┘
        │                    │                    │
        └────────────────────┼────────────────────┘
                             │
                    ┌────────┴────────┐
                    │     Redis       │
                    │   (库存缓存)    │
                    └────────┬────────┘
                             │
                    ┌────────┴────────┐
                    │    MySQL       │
                    │   (持久化)      │
                    └────────────────┘
```

### 核心设计要点

| 层级 | 组件 | 作用 |
|-----|------|-----|
| 接入层 | CDN + LVS + Nginx | 就近访问, 负载均衡 |
| 网关层 | API Gateway | 限流, 熔断, 鉴权 |
| 服务层 | 秒杀服务集群 | 业务逻辑 |
| 缓存层 | Redis | 库存预热, 热点数据 |
| 存储层 | MySQL | 持久化, 订单数据 |

### 关键技术方案

**1. 库存扣减 (Redis Lua)**
```lua
-- 原子操作保证库存一致性
local stock = redis.call('GET', KEYS[1])
if stock >= tonumber(ARGV[1]) then
  redis.call('DECRBY', KEYS[1], ARGV[1])
  return 1  -- 成功
end
return 0  -- 失败
```

**2. 限流策略**
- 接口限流: 10万/秒
- 用户限流: 100/分钟
- IP限流: 1000/分钟

**3. 消息队列解耦**
- 异步下单, 削峰填谷
- 防止超卖

### 性能指标

| 指标 | 目标值 |
|-----|-------|
| QPS | 10万+ |
| 响应时间 | < 100ms |
| 可用性 | 99.99% |
| 库存一致性 | 100% |

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 架构设计完成
设计质量: 完整、合理、可落地
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 总结

以上演示展示了天问-AGI的核心能力：

| 能力 | 演示用例 | 评分 |
|-----|---------|------|
| 代码生成 | 快排算法 | ✅ 正确 |
| 任务规划 | 电商用户模块 | ✅ 完整 |
| 情感交互 | Bug调试场景 | ✅ 同理 |
| 安全审查 | SQL注入检测 | ✅ 全面 |
| 架构设计 | 秒杀系统 | ✅ 专业 |
